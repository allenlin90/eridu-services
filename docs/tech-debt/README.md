# Tech Debt

Known implementation gaps and cleanup issues that should be fixed, but are not product ideation topics.

Use this register for accepted risk, consistency gaps, migration cleanup, and refactor debt. Use [ideation](../ideation/README.md) for new product or architecture ideas that need discovery, decision gates, or PRD promotion.

## Lifecycle

1. Create a tech-debt doc when a review accepts an issue as follow-up instead of blocking the current PR.
2. Keep each entry concrete: affected surface, current behavior, desired behavior, risk, and trigger to fix.
3. Link from PR descriptions or review notes when a known issue is intentionally deferred.
4. When fixed, update or delete the entry in the same PR that resolves it.

## Active Issues

| Issue | Area | Trigger To Fix | Related Context |
| --- | --- | --- | --- |
| [Audience → Open WebUI group mapping is only defined for ERISA](./erisa-platform-ops-manual-access-grants.md) | `ai/openwebui/access/audience-group-map.json`; `scripts/ai/creator-kb/upload_kb.py` | A non-ERISA collection is published, the Campaign team needs Platform PoC content, or the GM needs a collection they cannot currently reach (`org-general`'s automatic grant is suspended, not decided) | [Collection README](../../ai/openwebui/knowledge/erisa-platform-ops/README.md), [LLM knowledge base plan](../../ai/architecture/llm-knowledge-base-plan.md) |
| [Root `pnpm.overrides.multer` is a stopgap for NestJS lagging upstream](./multer-override-pending-nestjs-upstream.md) | `erify_api` — `@nestjs/platform-express` pins `multer@2.1.1` even at latest 11.1.x; two DoS advisories need `>=2.2.0` | `@nestjs/platform-express` ships `multer >=2.2.0` natively | — |
| [Admin Route Business Logic Bypass](./admin-route-business-logic-bypass.md) | `erify_api` admin mutations | Any admin mutation touches finance, assignment snapshots, actuals, lifecycle transitions, or cross-domain side effects | [Economics cost model](../domain/economics-cost-model.md), [Phase 4 remaining-work tracker](../roadmap/PHASE_4.md) |
| [`fact-extraction.service.ts` exceeds the file-size trigger](./fact-extraction-service-size.md) | `erify_api` fact-extraction pipeline | A fourth paired/hydrated extractor scope is added, or the file crosses ~1300 LOC | [fact-extraction-pipeline skill](../../.agents/skills/fact-extraction-pipeline/SKILL.md), [Phase 4 tracker](../roadmap/PHASE_4.md) |
| [Show-detail performance fields exposed to all members](./show-detail-performance-fields-all-members.md) | `erify_api` show detail authorization | Show-detail authz changes, more revenue facts added to detail, or product confirms whether per-platform GMV is manager-only | [AUTHORIZATION_GUIDE.md](../../apps/erify_api/docs/design/AUTHORIZATION_GUIDE.md), [PHASE_4.md](../roadmap/PHASE_4.md) |
| [Shared REST pagination limit diverges from the documented standard](./erify-api-shared-pagination-limit-unbounded.md) | `erify_api` list endpoints using `paginationBaseSchema` | Before another list endpoint adopts the shared schema, or when the shared pagination contract can be compatibility-audited | [API performance pagination pattern](../../.agents/skills/api-performance-optimization/references/03-pagination-patterns.md), [`erify_api` architecture](../../apps/erify_api/docs/ARCHITECTURE.md) |
| [Bulk schedule create and update map not-found errors differently](./erify-api-bulk-schedule-error-code-asymmetry.md) | `erify_api` schedule bulk-operation result contract | Before the response contract grows, a client branches on error codes, or the implementation changes | [`erify_api` architecture](../../apps/erify_api/docs/ARCHITECTURE.md) |
| [Cursor monorepo package guidance duplicates canonical instructions](./cursor-monorepo-package-rule-duplication.md) | Agent instruction adapters and supplementary memory | The next shared package-rule or Cursor-routing change | [Agent instruction maintenance skill](../../.agents/skills/agent-instruction-maintenance/SKILL.md) |
| [erify_api hardening program — deferred work](./erify-api-refactor-residuals.md) | `erify_api` money utility / Sheets identity / naming | A finance feature touches the money services, a Money-type decision is made, Sheets authentication changes, or the compensation slice is refactored | Hardening complete (PRs #157–#200); patterns/decisions in skills, this tracks deferred work only |
| [Studio↔client linkage gate unvalidated against real multi-studio data](./studio-client-linkage-single-studio-reality.md) | `erify_api` client-mechanic / task-template authorization — onboarding-sequencing gap and a one-way "once linked, never unlinked" ratchet | A second studio is onboarded, or any client relationship is ever reassigned away from a studio that previously held it | [CLIENT_MECHANICS_MANAGEMENT.md](../../apps/erify_studios/docs/CLIENT_MECHANICS_MANAGEMENT.md), [PHASE_6.md](../roadmap/PHASE_6.md) |
| [`AddStudioCreatorDialog` calls two async lookup hooks at the component level](./add-creator-dialog-dual-async-lookup-hooks.md) | `erify_studios` studio-creator-roster — `add-studio-creator-dialog.tsx` merged search/create dialog | A third dialog mode is added, or the unguarded catalog-query refetch during create mode causes an observed UX/rate-limit problem | [STUDIO_CREATOR_ONBOARDING.md](../../apps/erify_studios/docs/STUDIO_CREATOR_ONBOARDING.md), PR #225 |
| [Route-navigation search defaults are hardcoded per call site, not derived from the schema](./route-search-defaults-scattered-literals.md) | `erify_studios` route search-param contracts — ~35 `<Link>`/`navigate()` call sites hardcode `{ page: 1, limit: 10 }`-style literals instead of deriving them from the target route's own schema | A route's pagination/list default actually changes and its Links need hunting down to stay consistent — fix scoped to that one route, not preemptively | PR #257 |
| [`import-mc-list.ts` does not assign imported creators to a studio roster](./mc-list-import-script-no-roster-link.md) | `erify_api` scripts — one-off CSV importer writes `creators`/`users` but never `studio_creators` | The script (or a similar ad hoc roster-import script) is run again | PR #228 |
| [Material-asset upload version reservation can race](./upload-version-reservation-race.md) | `erify_api` upload presign bookkeeping — concurrent same-task, same-field reservations can select the same object version | Concurrent same-field uploads become a supported workflow, or every uploaded revision must be retained | `TaskRepository.reserveMaterialAssetUploadVersion` |
| [Schedule publish removal has no audit trail](./schedule-publish-removal-no-audit.md) | `erify_api` schedule publishing — remove-path writes `show_status_id` directly, bypassing the cancellation gate's `Audit` trail | `publishing.service.ts` diff/remove logic changes again, a second `GateKind` is introduced, or cancellation reporting needs reliable history | [Show Cancellation Gate](../../apps/erify_api/docs/SHOW_CANCELLATION_GATE.md), [schedule-publish-gate-unification ideation](../ideation/schedule-publish-gate-unification.md) |
| [Schedule publish restore has no audit trail](./schedule-publish-restore-no-audit.md) | `erify_api` schedule publishing — `toUpdate` restore loop bypasses the cancellation gate the same way the removal loop does | `publishing.service.ts` diff/restore logic changes again, or cancellation/restoration reporting is added | [Show Cancellation Gate](../../apps/erify_api/docs/SHOW_CANCELLATION_GATE.md), [schedule-publish-removal-no-audit.md](./schedule-publish-removal-no-audit.md) |
| [Schedule publish writes impact audits one show at a time](./schedule-publish-sequential-audit-writes.md) | `erify_api` schedule publishing — remove-path and confirmed-future-update loops write one `Audit` row per show sequentially instead of batching | A publish approaches the 30s transaction timeout, `AuditRepository` gains a bulk-create primitive, or schedule sizes/publish frequency grow materially | [schedule-publish-removal-no-audit.md](./schedule-publish-removal-no-audit.md) |
| [Applying a held-back conflict doesn't write FK-backed field changes](./schedule-conflict-apply-fk-fields-not-written.md) | `erify_api` schedule-publish conflict resolution — `toShowUpdateData` only applies scalar fields (name/time/metadata) from a held-back diff, not FK-backed fields (client/studio/room/type/status/standard) | An FK-backed held-back diff is actually applied in practice and the field not landing causes a real workflow gap | [STUDIO_SHOW_MANAGEMENT.md](../../apps/erify_api/docs/STUDIO_SHOW_MANAGEMENT.md) |
| [Schedule-publish-impacts list pages its sources independently, not merge-sorted](./schedule-publish-impacts-list-not-merge-sorted.md) | `erify_api` schedule-publish conflict resolution — `listSchedulePublishImpacts` concatenates up to three independently-paginated queries (confirmed, pending-stale, resolved-stale) instead of one cross-kind merge-sorted page | `stale_conflict` volume grows enough that a planner observes a missing/duplicated row across a page boundary | [schedule-publish-sequential-audit-writes.md](./schedule-publish-sequential-audit-writes.md) |
| [Pending-stale-conflict count recomputes the full result set](./schedule-publish-pending-stale-count-recompute.md) | `erify_api` schedule-publish review queue — `countPendingStaleConflictsForStudio` materializes every historical stale-conflict row to return `.length`; runs twice per KPI-summary render | Impacts list/summary latency becomes measurable for a studio, or the merge-sorted pagination rework lands | [schedule-publish-impacts-list-not-merge-sorted.md](./schedule-publish-impacts-list-not-merge-sorted.md) |
| [Studios filter groups and show-time cells still lack cross-feature sharing](./studios-filter-dropdown-and-time-cell-duplication.md) | `erify_studios` operational surfaces — drift-prone cores now extracted (`toggleArrayValue`, `MultiSelectCheckboxItems`, relocated `CheckboxFilterGroup`, `show-time-format` helpers; 2026-07-24); only optional single-widget unification remains | A surface needs a new inline/dropdown multi-select (reuse the shared primitive), or a deliberate UX pass normalizes the filter surfaces / show-time cells to one shape — a visual change, not preemptive | PR #310 review findings 8–9; schedule-publish UX correction |
| [Held-back creator/platform diffs carry no display name](./schedule-conflict-held-back-creators-platforms-no-display-name.md) | `erify_api`/`erify_studios` schedule-publish conflict resolution — `held_back.show_creators[]`/`show_platforms[]` carry only a bare uid, unlike `show_fields`' resolved `{uid, name}` | A studio reports the review panel's creator/platform rows are hard to identify in practice | [schedule-conflict-apply-fk-fields-not-written.md](./schedule-conflict-apply-fk-fields-not-written.md) |
| [Scene QC's first-load operational date still comes from the browser clock](./scene-qc-default-operational-date-browser-clock.md) | `erify_studios` Scene QC Daily Review — PR #339 made the window server-authoritative, but the initial date has no specified source; `getCurrentOperationalDate()` correctly converts via `Intl.DateTimeFormat` on the shared timezone constant, so only a skewed system clock (not device timezone) can pick the wrong default | A headless caller needs the current operational day, or a skewed-clock wrong default is reported in practice | [Scene QC](../features/scene-qc.md), [studio-config-settings §6](../ideation/studio-config-settings.md), PR #339 review |
| [Scene Profile save performs an R2 HeadObject probe inside an open Postgres transaction](./scene-qc-profile-save-r2-probe-inside-transaction.md) | `erify_api` Scene QC — `SceneProfileService.saveProfileForClient` calls `StorageService.headObject` (a network round trip) before any Prisma write, inside `@Transactional()` | R2 timeouts are observed in practice, or before Scene QC handles real production write traffic | PR #347 review |
| [Scene QC daily summary/items each re-run the full evidence resolver, and items paginates in memory](./scene-qc-daily-read-models-duplicate-evidence-resolution.md) | `erify_api` Scene QC — `SceneQcQueryService.getDailySummary`/`.listDailyItems` both call `resolveForShows` independently; `listDailyItems` slices in memory after loading the full eligible set | Operational days routinely approach the 500-Show cap, or `listDailyItems` latency becomes visible in practice | PR #348 review |
| [Scene QC report status recomputes the eligible Show set on every request](./scene-qc-report-status-recomputes-eligible-set.md) | `erify_api` Scene QC — distinguishing a report's `CURRENT` from `STALE` status re-derives the operational day's current eligible set rather than reading a cached decision | Report/Records-detail latency becomes visible, or operational-day Show counts routinely approach the 500-row cap | PR #350 review |
| [Scene QC confirmation items cascade-delete with their Show/Client/Review](./scene-qc-confirmation-items-cascade-with-show-delete.md) | `erify_api` Scene QC — a hard Show/Client/`SceneQcReview` delete would silently remove rows from an immutable historical confirmation and change a shipped report's totals; no hard-delete path exists today | A hard-delete path for Show or Client is introduced, or report immutability becomes a compliance requirement | PR #350 review |
| [Two operator scripts likely can't bootstrap (missing `ConfigModule.forRoot()`)](./erify-api-scripts-missing-config-module.md) | `erify_api` standalone scripts — `consolidate-duplicate-mechanics.ts` and `backfill-product-promotion-mechanics.ts` share the same bootstrap-module gap that broke the (now-fixed) Scene QC cutover scripts | Before either script is next run for a real operator task | PR #343 (main integration PR) wrap-up |
