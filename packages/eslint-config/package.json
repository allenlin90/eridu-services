{
  "name": "@eridu/eslint-config",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./eslint.config.js",
    "./create-config": "./create-config.js"
  },
  "scripts": {
    "build": "echo \"No build needed for eslint-config\" && exit 0",
    "dev": "echo \"No dev mode for eslint-config - keeping process alive\" && node -e \"setInterval(() => {}, 1000)\"",
    "lint": "echo \"No linting needed for eslint-config\" && exit 0",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^3.12.1",
    "eslint": "^9.39.1",
    "eslint-plugin-format": "^0.1.3",
    "eslint-plugin-simple-import-sort": "^12.1.1"
  }
}
