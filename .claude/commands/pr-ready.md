---
description: Claude compatibility alias for the shared pr-ready skill and its canonical merge-readiness workflow
argument-hint: "[PR number or branch — optional, defaults to current branch vs origin/master]"
---

# PR Ready

This is the Claude Code compatibility alias for the shared `pr-ready` skill.

Read `.agents/skills/pr-ready/SKILL.md` completely and execute it with `$ARGUMENTS` as the optional PR or branch target. The shared skill owns routing and `.agents/workflows/pr-review.md` remains the canonical process.
