# Pinned so an unrelated commit under infra/nao/** (which triggers a rebuild via the
# /infra/nao/** watch pattern) can't silently pull a newer, unreviewed nao release.
# To intentionally upgrade: check https://hub.docker.com/r/getnao/nao/tags for the new
# version tag's digest, bump both below.
FROM getnao/nao:0.3.3@sha256:717d33eb292cd29de28f15927136c27f32b6a253c6fdb641769f5b3f574f7765
