/**
 * Language switcher — hidden until project-wide Paraglide migration.
 *
 * New creators UI uses inline English (same convention as erify_studios compensations).
 */

/**
 * @deprecated English-only portal; re-enable when Paraglide migration lands.
 */
export function LanguageSwitcher() {
  return null;
}
