interface:
  display_name: "PR Ready"
  short_description: "Run the canonical pre-merge readiness review"
  default_prompt: "Use $pr-ready to review the current pull request and return a READY or NOT READY verdict."
policy:
  allow_implicit_invocation: false
