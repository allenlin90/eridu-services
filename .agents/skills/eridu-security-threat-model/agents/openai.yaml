interface:
  display_name: "Security Threat Model"
  short_description: "Repo-grounded threat modeling and abuse-path analysis"
  default_prompt: "Use $eridu-security-threat-model to create a repo-grounded threat model with prioritized abuse paths and mitigations."
policy:
  allow_implicit_invocation: false
