---
description: Core behavior guidelines and workflows for coding agents in Eridu Services.
globs: *
---
# Eridu Services - General Agent Guidelines

Canonical guidance lives in `AGENTS.md` at the repo root. Read that file first if your tool supports it (Cursor does not auto-load `AGENTS.md` — the essentials below cover what you need).

## Workflow

- **Skills:** before implementing any feature, discover and load the relevant skill from `.agents/skills/<skill-name>/SKILL.md`. Each `SKILL.md` has a name and description in its frontmatter.
- **Verification:** run `.agents/workflows/verification.md` after every code change.
- **Knowledge sync:** run the `knowledge-sync` skill after feature delivery or refactors.
- **PWA changes:** follow `.agents/workflows/pwa-migration.md` + `pwa-best-practices` skill.

## Core Engineering Rules

- Never expose DB internal IDs from API responses. Use UID-based external IDs (`{prefix}_{nanoid}`).
- Backend (`erify_api`) follows repository → service → controller separation.
- Use Zod schemas; transform snake_case (API) ↔ camelCase (service/domain) at layer boundaries.
- Prefer bulk DB operations and relation includes over N+1 patterns.
- Maintain strict typing. Do not bypass with `any` or `@ts-ignore` unless explicitly requested.
- Keep internal package dependency spec as `workspace:*`.
- Migrations must be generated by official tooling (Prisma for `erify_api`, Drizzle for `eridu_auth`). Do not hand-write or rewrite already-deployed migrations.
- Store facts on the narrowest entity they describe. Do not persist derived finance totals on operational tables.
